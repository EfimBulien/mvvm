<Page x:Class="generator.EditTestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="EditTestPage" Height="Auto" Width="Auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="TestGrid" ItemsSource="{Binding Tests}" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Row="0">
        </DataGrid>
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Grid.Row="1">
            <Button x:Name="AddButton" Content="Добавить" Foreground="White" Command="{Binding SortingCommand}"/>
            <Button x:Name="DelButton" Content="Удалить" Foreground="White" Command="{Binding DeleteCommand}"/>
            <Button x:Name="UpButton" Content="Сохранить" Foreground="White" Command="{Binding UpdateCommand}"/>
        </StackPanel>
    </Grid>
</Page>
